import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        int students = scanner.nextInt() + scanner.nextInt() + scanner.nextInt();
        scanner.nex

        System.out.println((students % 2) + (students / 2));
    }
}
